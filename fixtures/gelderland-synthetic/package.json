{
  "name": "@civic-sentry/demo-gelderland-synthetic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "demo": "node dist/run-demo.js",
    "test": "echo \"no unit tests here — this package IS the end-to-end test, run via demo\"",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@civic-sentry/schema": "workspace:*",
    "@civic-sentry/core": "workspace:*",
    "@civic-sentry/collectors": "workspace:*"
  }
}
